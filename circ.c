#include<stdio.h>
int main()
{
int a,b,c,i,j;
printf("Enter the calculation:");
scanf("%d",&a);
printf("Enter the n value");
scanf("%d",&b);
printf("Enter the pair:");
scanf("%d",&c);
for(i=0;i<2*c;i++);
{
for(j=1;j=2*b-a;j++)
if(c!=j)
{
printf("Enter the pair %d",c);
printf("Enter the for day %d%d",j++,c);
}
}
printf("Enter the calculation%d",a);
return 0;
}
